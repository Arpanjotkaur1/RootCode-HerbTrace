# dependencies
/node_modules

# next.js build output
/.next/
/out/

# env files (never commit real secrets)
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# NOTE: public/models (trained classifier files) is intentionally NOT ignored here.
# Those files can get large (multi-MB). Khushi: decide whether to commit them so
# teammates can pull the trained model directly, or gitignore them and share via
# another channel (Drive/Discord) instead. See root README.md for the flag.
